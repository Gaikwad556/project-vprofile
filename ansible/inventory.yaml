all:
  hosts:
    Kubernetes_Main:
      ansible_host: 10.0.1.6
      ansible_ssh_private_key_file: keys/ansible_ssh_key

    Jenkins_Main:
      ansible_host: 10.0.2.66
      ansible_ssh_private_key_file: keys/jenkins_ssh_key

    Docker_Main:
      ansible_host: 10.0.2.218
      ansible_ssh_private_key_file: keys/docker_ssh_key

  vars:
    ansible_user: ubuntu

  children:
    kubernetes:
      hosts:
        Kubernetes_Main:
    docker:
      hosts:
        Docker_Main:
    jenkins:
      hosts:
        Jenkins_Main:
